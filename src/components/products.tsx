import { View, Text, } from "react-native";

type Props = {
    title: string,
    image?: string,
    description: string,
}

export function Products( { title, image, description, ...rest}: Props) {
    <View>

    </View>
}